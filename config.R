# Instalar las librerías para que estén en el archivo renv
## Llamanr el paquete renv
library(renv)

## Instalar las librerias
renv::install("dplyr")
install("tidyveyrse")
install("tidyr")
install("pins")
install("googlesheets4")
install("rmarkdown")

